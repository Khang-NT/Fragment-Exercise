<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorButtonNormal">@color/colorAccent</item>
    </style>

    <declare-styleable name="SnowEffect">
        <attr name="snowColor" format="integer|color" />
        <attr name="numSnowObjects" format="integer" />
        <attr name="minRadius" format="dimension" />
        <attr name="maxRadius" format="dimension" />
        <attr name="windSpeed" format="float" />
        <!-- Number of frame per second.
            @param numFPS Recommend set at least 10 FPS-->
        <attr name="numFPS" format="integer" />
    </declare-styleable>

</resources>
